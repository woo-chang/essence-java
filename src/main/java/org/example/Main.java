package org.example;

import org.example.operator.ComparativeOperator;
import org.example.operator.UnionOperator;
import org.example.variable.Swap;

public class Main {

    public static void main(String[] args) {
        ComparativeOperator comparativeOperator = new ComparativeOperator();
        comparativeOperator.run();
    }

}